# Server-only production secrets/configuration.
# Never commit real values here.

DATABASE_URL=

# Better Auth server secret, if enabled by the deployment.
BETTER_AUTH_SECRET=

# Optional TURN configuration. The server exposes these only through
# /api/rtc/config; do not prefix them with VITE_.
TURN_URLS=
TURN_USERNAME=
TURN_CREDENTIAL=
